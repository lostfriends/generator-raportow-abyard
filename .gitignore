# Zależności instalowane przy buildzie — nie trzymamy w repo
node_modules/
package-lock.json

# Wynik builda — generowany przez Netlify, nie commitujemy

# Pliki pośrednie / systemowe
*.log
.DS_Store

# Ręcznie pakowany build do deployu manualnego
abyard-dist.zip
